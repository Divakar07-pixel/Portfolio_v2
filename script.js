const loader=document.getElementById('page-loader');
const canvas=document.getElementById('orb-canvas');
const stage=document.querySelector('.orb-stage');
const hero=document.querySelector('.hero');
const heroCopy=document.querySelector('.hero-copy');
const portalCore=document.querySelector('.portal-entry');
const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if(loader&&!reduced)document.body.classList.add('portal-lock');
if(canvas&&stage){const ctx=canvas.getContext('2d');let width=0,height=0,dpr=1,mouseX=0,mouseY=0,targetX=0,targetY=0,points=[];const POINTS=135;function createScene(){const radius=Math.min(width,height)*.34;points=Array.from({length:POINTS},()=>{const theta=Math.acos(1-2*Math.random()),phi=Math.random()*Math.PI*2;return{x:radius*Math.sin(theta)*Math.cos(phi),y:radius*Math.cos(theta),z:radius*Math.sin(theta)*Math.sin(phi),size:.45+Math.random()*1.05};});}function resize(){const rect=stage.getBoundingClientRect();width=rect.width;height=rect.height;dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=`${width}px`;canvas.style.height=`${height}px`;ctx.setTransform(dpr,0,0,dpr,0,0);createScene();}function project(p,ry,rx){const cy=Math.cos(ry),sy=Math.sin(ry),cx=Math.cos(rx),sx=Math.sin(rx),x1=p.x*cy-p.z*sy,z1=p.x*sy+p.z*cy,y1=p.y*cx-z1*sx,z2=p.y*sx+z1*cx,perspective=1+z2/(Math.min(width,height)*1.55);return{x:width*.5+x1*perspective,y:height*.49+y1*perspective,z:z2,size:p.size*perspective};}function draw(time){ctx.clearRect(0,0,width,height);targetX+=(mouseX-targetX)*.035;targetY+=(mouseY-targetY)*.035;const projected=points.map(p=>project(p,time*.00016+targetX*.22,Math.sin(time*.0002)*.13+targetY*.12)),maxDistance=Math.min(width,height)*.14;ctx.lineWidth=.42;for(let i=0;i<projected.length;i++)for(let j=i+1;j<projected.length;j++){const a=projected[i],b=projected[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<maxDistance&&Math.abs(a.z-b.z)<125){ctx.strokeStyle=`rgba(255,255,255,${Math.max(.025,.13-d/(maxDistance*1.45))})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}}projected.sort((a,b)=>a.z-b.z).forEach(p=>{const depth=Math.max(0,Math.min(1,(p.z+260)/520));ctx.fillStyle=`rgba(255,255,255,${.18+depth*.7})`;ctx.beginPath();ctx.arc(p.x,p.y,Math.max(.45,p.size),0,Math.PI*2);ctx.fill();});requestAnimationFrame(draw);}window.addEventListener('resize',resize);window.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect();if(!r.width||!r.height)return;mouseX=(e.clientX-r.left)/r.width-.5;mouseY=(e.clientY-r.top)/r.height-.5;});document.documentElement.classList.add('js-ready');resize();requestAnimationFrame(draw);const items=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target);}}),{threshold:.15});items.forEach(i=>observer.observe(i));const summary=document.querySelector('.summary-section');if(summary){const summaryObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');summaryObserver.unobserve(entry.target);}}),{threshold:.2,rootMargin:'0px 0px -8% 0px'});summaryObserver.observe(summary);}}}
if(loader&&stage&&!reduced){let entered=false,current=0,target=0,raf=false;const clamp=(v,a,b)=>Math.min(b,Math.max(a,v));const ease=v=>v<.5?2*v*v:1-Math.pow(-2*v+2,2)/2;const moveStageIntoLoader=()=>{if(!loader.contains(stage))loader.appendChild(stage);stage.classList.add('portal-in-loader');if(heroCopy&&!loader.contains(heroCopy)){heroCopy.classList.add('portal-copy');loader.appendChild(heroCopy);}loader.classList.add('portal-ready');document.body.classList.add('portal-active');window.dispatchEvent(new Event('resize'));};const ready=()=>moveStageIntoLoader();if(document.readyState==='complete')ready();else window.addEventListener('load',ready,{once:true});const setProgress=v=>{target=clamp(v,0,1);if(!raf){raf=true;requestAnimationFrame(tick);}};function tick(){current+=(target-current)*.105;if(Math.abs(target-current)<.0005)current=target;const p=ease(current),scale=1+p*.72,opacity=1-clamp((p-.9)/.1,0,1),blur=clamp((p-.82)/.18,0,1)*1.2,haze=1+p*1.55;loader.style.setProperty('--loader-portal-scale',scale.toFixed(3));loader.style.setProperty('--loader-portal-opacity',opacity.toFixed(3));loader.style.setProperty('--loader-portal-blur',`${blur.toFixed(2)}px`);loader.style.setProperty('--loader-haze-scale',haze.toFixed(3));loader.style.setProperty('--portal-progress',p.toFixed(4));if(portalCore){portalCore.style.setProperty('--portal-core-opacity',(p>.68?clamp((p-.68)/.2,0,1):0).toFixed(3));portalCore.style.setProperty('--portal-core-scale',(1+p*2.2).toFixed(3));}if(heroCopy){const reveal=clamp((p-.16)/.56,0,1),scaleCopy=.74+reveal*.26,y=(1-reveal)*48,x=(1-reveal)*6,actions=clamp((p-.62)/.25,0,1);heroCopy.style.setProperty('--portal-copy-opacity',reveal.toFixed(3));heroCopy.style.setProperty('--portal-copy-scale',scaleCopy.toFixed(3));heroCopy.style.setProperty('--portal-copy-y',`${y.toFixed(1)}px`);heroCopy.style.setProperty('--portal-copy-x',`${x.toFixed(1)}vw`);heroCopy.style.setProperty('--portal-copy-clip',`${(reveal*150).toFixed(1)}%`);heroCopy.style.setProperty('--portal-actions-opacity',actions.toFixed(3));heroCopy.style.setProperty('--portal-actions-y',`${((1-actions)*18).toFixed(1)}px`);}if(current!==target)requestAnimationFrame(tick);else raf=false;if(!entered&&current>.985)finish();}function finish(){entered=true;if(heroCopy){heroCopy.classList.remove('portal-copy','reveal');heroCopy.style.removeProperty('--portal-copy-opacity');heroCopy.style.removeProperty('--portal-copy-scale');heroCopy.style.removeProperty('--portal-copy-y');heroCopy.style.removeProperty('--portal-copy-x');heroCopy.style.removeProperty('--portal-copy-clip');heroCopy.style.removeProperty('--portal-actions-opacity');heroCopy.style.removeProperty('--portal-actions-y');heroCopy.style.setProperty('opacity','1');heroCopy.style.setProperty('transform','none');heroCopy.style.setProperty('clip-path','none');hero.appendChild(heroCopy);}stage.classList.remove('portal-in-loader');loader.classList.add('portal-entered');document.body.classList.remove('portal-lock');document.body.classList.remove('portal-active');setTimeout(()=>{if(!hero.contains(stage))hero.appendChild(stage);stage.style.removeProperty('--loader-portal-scale');stage.style.removeProperty('--loader-portal-opacity');stage.style.removeProperty('--loader-portal-blur');stage.style.removeProperty('--loader-haze-scale');window.dispatchEvent(new Event('resize'));},650);}const advance=amount=>{if(entered)return;setProgress(clamp(target+amount,0,1));};const onWheel=e=>{if(entered||e.ctrlKey)return;if(e.cancelable)e.preventDefault();advance(Math.min(.17,Math.abs(e.deltaY)/930));};const onKey=e=>{if(entered)return;if(['ArrowDown','PageDown',' ','End'].includes(e.key)){e.preventDefault();advance(.17);}};let touchY=0;const onTouchStart=e=>{touchY=e.touches[0].clientY;};const onTouchMove=e=>{if(entered)return;const dy=touchY-e.touches[0].clientY;if(dy>3){if(e.cancelable)e.preventDefault();advance(Math.min(.2,dy/540));touchY=e.touches[0].clientY;}};window.addEventListener('wheel',onWheel,{passive:false});window.addEventListener('keydown',onKey,{passive:false});window.addEventListener('touchstart',onTouchStart,{passive:true});window.addEventListener('touchmove',onTouchMove,{passive:false});}

/* Global scroll choreography for the Home sections. Each section enters once, with a restrained motion language rather than repeating the portal effect. */
if(!reduced&&'IntersectionObserver'in window){const sections=document.querySelectorAll('.scroll-section');const motionObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target;el.classList.add('is-inview');if(el.classList.contains('projects-section'))el.classList.add('projects-inview');if(el.classList.contains('skills-section'))el.classList.add('skills-inview');if(el.classList.contains('contact'))el.classList.add('contact-inview');motionObserver.unobserve(el);}),{threshold:.12,rootMargin:'0px 0px -10% 0px'});sections.forEach(section=>motionObserver.observe(section));}
if(reduced&&loader)window.setTimeout(()=>loader.classList.add('is-hidden'),700);