const loader=document.getElementById('page-loader');
const canvas=document.getElementById('orb-canvas');
const stage=document.querySelector('.orb-stage');
const hero=document.querySelector('.hero');
const heroCopy=document.querySelector('.hero-copy');
const portalCore=document.querySelector('.portal-entry');
const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if(loader&&!reduced){document.body.classList.add('portal-lock');const prompt=document.createElement('div');prompt.className='loader-scroll-prompt';prompt.innerHTML='<span>SCROLL TO ENTER</span>';loader.appendChild(prompt);}
if(canvas&&stage){const ctx=canvas.getContext('2d');let width=0,height=0,dpr=1,mouseX=0,mouseY=0,targetX=0,targetY=0,points=[],stars=[];const POINTS=135,STARS=85;function createScene(){const radius=Math.min(width,height)*.34;points=Array.from({length:POINTS},()=>{const theta=Math.acos(1-2*Math.random()),phi=Math.random()*Math.PI*2;return{x:radius*Math.sin(theta)*Math.cos(phi),y:radius*Math.cos(theta),z:radius*Math.sin(theta)*Math.sin(phi),size:.45+Math.random()*1.05};});stars=Array.from({length:STARS},()=>({x:width*(.1+Math.random()*.82),y:height*(.08+Math.random()*.84),r:.3+Math.random()*.8,phase:Math.random()*Math.PI*2}));}function resize(){const rect=stage.getBoundingClientRect();width=rect.width;height=rect.height;dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=`${width}px`;canvas.style.height=`${height}px`;ctx.setTransform(dpr,0,0,dpr,0,0);createScene();}function project(p,ry,rx){const cy=Math.cos(ry),sy=Math.sin(ry),cx=Math.cos(rx),sx=Math.sin(rx),x1=p.x*cy-p.z*sy,z1=p.x*sy+p.z*cy,y1=p.y*cx-z1*sx,z2=p.y*sx+z1*cx,perspective=1+z2/(Math.min(width,height)*1.55);return{x:width*.58+x1*perspective,y:height*.49+y1*perspective,z:z2,size:p.size*perspective};}function draw(time){ctx.clearRect(0,0,width,height);targetX+=(mouseX-targetX)*.035;targetY+=(mouseY-targetY)*.035;stars.forEach(s=>{const a=.2+(Math.sin(time*.001+s.phase)+1)*.22;ctx.fillStyle=`rgba(255,255,255,${a})`;ctx.beginPath();ctx.arc(s.x+targetX*10,s.y+targetY*10,s.r,0,Math.PI*2);ctx.fill();});const projected=points.map(p=>project(p,time*.00016+targetX*.22,Math.sin(time*.0002)*.13+targetY*.12)),maxDistance=Math.min(width,height)*.14;ctx.lineWidth=.42;for(let i=0;i<projected.length;i++)for(let j=i+1;j<projected.length;j++){const a=projected[i],b=projected[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<maxDistance&&Math.abs(a.z-b.z)<125){ctx.strokeStyle=`rgba(255,255,255,${Math.max(.025,.13-d/(maxDistance*1.45))})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}}projected.sort((a,b)=>a.z-b.z).forEach(p=>{const depth=Math.max(0,Math.min(1,(p.z+260)/520));ctx.fillStyle=`rgba(255,255,255,${.18+depth*.7})`;ctx.beginPath();ctx.arc(p.x,p.y,Math.max(.45,p.size),0,Math.PI*2);ctx.fill();});requestAnimationFrame(draw);}window.addEventListener('resize',resize);window.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect();mouseX=(e.clientX-r.left)/r.width-.5;mouseY=(e.clientY-r.top)/r.height-.5;});document.documentElement.classList.add('js-ready');resize();requestAnimationFrame(draw);const items=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target);}}),{threshold:.15});items.forEach(i=>observer.observe(i));}}
if(loader&&stage&&!reduced){const moveStageIntoLoader=()=>{if(!loader.contains(stage))loader.appendChild(stage);stage.classList.add('portal-in-loader');loader.classList.add('portal-ready');loader.querySelector('.loader-word').textContent='READY';document.body.classList.add('portal-active');window.dispatchEvent(new Event('resize'));};if(document.readyState==='complete')moveStageIntoLoader();else window.addEventListener('load',moveStageIntoLoader,{once:true});let entered=false,current=0,target=0,raf=false;const clamp=(v,a,b)=>Math.min(b,Math.max(a,v));const ease=v=>v<.5?2*v*v:1-Math.pow(-2*v+2,2)/2;const setProgress=v=>{target=clamp(v,0,1);if(!raf){raf=true;requestAnimationFrame(tick);}};function tick(){current+=(target-current)*.11;if(Math.abs(target-current)<.0006)current=target;const p=ease(current);const scale=1+p*1.25,opacity=1-clamp((p-.78)/.22,0,1),blur=clamp((p-.72)/.28,0,1)*1.6,haze=1+p*1.8;loader.style.setProperty('--loader-portal-scale',scale.toFixed(3));loader.style.setProperty('--loader-portal-opacity',opacity.toFixed(3));loader.style.setProperty('--loader-portal-blur',`${blur.toFixed(2)}px`);loader.style.setProperty('--loader-haze-scale',haze.toFixed(3));loader.style.setProperty('--portal-progress',p.toFixed(4));if(hero){const reveal=clamp((p-.24)/.46,0,1);hero.style.setProperty('--hero-copy-opacity',(reveal*(1-clamp((p-.88)/.12,0,1)*.12)).toFixed(3));hero.style.setProperty('--hero-copy-scale',(.88+reveal*.12).toFixed(3));hero.style.setProperty('--hero-copy-y',`${((1-reveal)*45).toFixed(1)}px`);hero.style.setProperty('--hero-copy-clip',`${(reveal*110).toFixed(1)}%`);}if(current!==target)requestAnimationFrame(tick);else raf=false;if(!entered&&current>.985){entered=true;stage.classList.remove('portal-in-loader');loader.classList.add('portal-entered');document.body.classList.remove('portal-lock');document.body.classList.remove('portal-active');setTimeout(()=>{if(hero&&!hero.contains(stage))hero.appendChild(stage);stage.style.removeProperty('--loader-portal-scale');stage.style.removeProperty('--loader-portal-opacity');stage.style.removeProperty('--loader-portal-blur');stage.style.removeProperty('--loader-haze-scale');window.dispatchEvent(new Event('resize'));},500);}}const advance=amount=>{if(entered)return;setProgress(clamp(target+amount,0,1));};const onWheel=e=>{if(entered||e.ctrlKey)return;if(e.cancelable)e.preventDefault();advance(Math.min(.18,Math.abs(e.deltaY)/900));};const onKey=e=>{if(entered)return;if(['ArrowDown','PageDown',' ','End'].includes(e.key)){e.preventDefault();advance(.18);}};let touchY=0;const onTouchStart=e=>{touchY=e.touches[0].clientY;};const onTouchMove=e=>{if(entered)return;const dy=touchY-e.touches[0].clientY;if(dy>4){if(e.cancelable)e.preventDefault();advance(Math.min(.22,dy/520));touchY=e.touches[0].clientY;}};window.addEventListener('wheel',onWheel,{passive:false});window.addEventListener('keydown',onKey,{passive:false});window.addEventListener('touchstart',onTouchStart,{passive:true});window.addEventListener('touchmove',onTouchMove,{passive:false});}if(reduced&&loader)window.setTimeout(()=>loader.classList.add('is-hidden'),700);
